<header class="main-header">
  <div class="header-content">
    <div class="login-button-container">
      <div class="login-button">
        <i class="fa-solid fa-user-shield" aria-hidden="true"></i>
        <span>Admin</span>
        <span class="plan-pill">Free</span>
      </div>
    </div>

    <button class="burgir" aria-label="Toggle nav">
      <span class="line"></span>
      <span class="line"></span>
      <span class="line"></span>
    </button>

    <ul class="links">
      <li><a href="/admin">Dashboard</a></li>
      <li><a href="/adminprogram">Program</a></li>
      <li><a href="/admininfo">Informasi</a></li>
    </ul>
  </div>
</header>

<style>
  .parent-nav a {
    text-decoration: none;
    color: #000;
    font-weight: 500;
    font-size: 1.1rem;
    transition: color 0.3s ease;
  }

  .parent-nav {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: 5rem;
  }

  .burgir {
    display: none;
    flex-direction: column;
    justify-content: space-around;
    width: 2rem;
    height: 2rem;
    background: transparent;
    border: none;
    cursor: pointer;
    z-index: 10;
  }

  .burgir .line {
    width: 2rem;
    height: 0.25rem;
    background: #333;
    border-radius: 10px;
    transition: all 0.3s linear;
    position: relative;
    transform-origin: 1px;
  }

  .links {
    list-style: none;
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-right: 32px;
  }

  .links a{
    text-decoration: none;
    color: #085895;
    font-weight: 500;
  }

  .links a:hover{ color: #2563eb; }


  @media (max-width: 768px) {
    .parent-nav {
      flex-direction: column;
      align-items: flex-end;
    }

    .burgir {
      display: flex;
      position: fixed;
      top: 5rem;
      left: 1rem;
      z-index: 9999;
    }
    /* Transform burger into an X when active and position top-right */
    .burgir {
      right: 1rem;
      left: auto;
    }
    .burgir.is-active .line:nth-child(1) {
      transform: rotate(45deg) translate(4px, 5px);
      background: #1e40af;
    }
    .burgir.is-active .line:nth-child(2) {
      opacity: 0;
    }
    .burgir.is-active .line:nth-child(3) {
      transform: rotate(-45deg) translate(3px, -5px);
      background: #1e40af;
    }
    .links {
      display: none;
      flex-direction: column;
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100vh;
      background: rgba(255, 255, 255, .95);
      z-index: 5;
      padding-top: 5rem;
    }

    .links.is-active {
      display: flex;
      align-items: center;
      gap: 2rem;
    }

    /* Remove any extra close/cancel buttons inside the menu (keep only burger-transformed X) */
    .links button,
    .links .exit,
    .links .close,
    .links .btn-close,
    .links .fa-times,
    .links [aria-label="Close"],
    .links .icon-close {
      display: none !important;
    }
  }

  .main-header {
    position: sticky; top: 0; z-index: 50;
    padding: 10px 16px;
    backdrop-filter: saturate(180%) blur(8px);
    background: rgba(255,255,255,.8);
    border-bottom: 1px solid rgba(0,0,0,.06);
    transition: box-shadow .2s ease;
  }

  .header-content { display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: 12px; max-width: 1200px; margin: 0 auto; }

  /* Remove old unused exit styles to avoid confusion */

  .ul-nav {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-top: 1rem;
  }

  .li-nav a {
    text-decoration: none;
    color: #085895;
    font-weight: 500;
  }

  .login-button { display: inline-flex; align-items: center; gap: 10px; padding: 10px 14px; border-radius: 12px; background: #fff; border: 1px solid #e5e7eb; box-shadow: 0 4px 14px rgba(0,0,0,.06); }
  .plan-pill { padding: 4px 8px; border-radius: 999px; background: rgba(34,211,238,.15); color: #22d3ee; font-weight: 700; font-size: .8rem; border: 1px solid rgba(34,211,238,.35); }
  /* removed premium/upgrade buttons per request */

  /* Locked/premium blur utility */
  .locked { position: relative; filter: blur(2px); pointer-events: none; user-select: none; }
  .locked::after { content: "Upgrade to Unlock"; position: absolute; inset: 0; display: grid; place-items: center; color: #fff; font-weight: 700; letter-spacing: .3px; background: rgba(2,6,23,.45); border-radius: 12px; }
  /* here is the navigation */
</style>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const burgerMenu = document.querySelector(".burgir") as HTMLElement | null;
    const navLinks = document.querySelector(".links") as HTMLElement | null;
    const closeBtn = document.querySelector('.close-nav') as HTMLButtonElement | null;
    const body = document.body as HTMLBodyElement;
    const header = document.querySelector('.main-header') as HTMLElement | null;

    if (burgerMenu && navLinks) {
      burgerMenu.addEventListener("click", () => {
        navLinks.classList.toggle("is-active");
        burgerMenu.classList.toggle("is-active");
        body.classList.toggle("no-scroll");
      });
    }

    // Close button behavior: hide menu and reveal burger
    if (closeBtn && burgerMenu && navLinks) {
      closeBtn.addEventListener('click', () => {
        navLinks.classList.remove('is-active');
        burgerMenu.classList.remove('is-active');
        body.classList.remove('no-scroll');
      });
    }

    // Shadow on scroll
    window.addEventListener('scroll', () => {
      if (!header) return;
      header.style.boxShadow = window.scrollY > 4 ? '0 6px 18px rgba(2,6,23,.06)' : 'none';
    });
  });
</script>
