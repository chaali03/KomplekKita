---
import Layout from "~/layouts/Layout.astro";
---

<Layout title="Dashboard - KomplekKita">
  <main class="dashboard-container">
    <div class="dashboard-header">
      <h1>Dashboard</h1>
      <p>Selamat datang di dashboard KomplekKita!</p>
    </div>
    
    <div class="dashboard-content">
      <div class="welcome-card">
        <h2>Registrasi Komplek Berhasil!</h2>
        <p>Anda telah berhasil menyelesaikan registrasi komplek. Sekarang Anda dapat mengakses semua fitur KomplekKita.</p>
        <div class="action-buttons">
          <a href="/admin" class="btn btn-primary">Panel Admin</a>
          <button id="logoutBtn" class="btn btn-secondary">Logout</button>
        </div>
      </div>
    </div>

    <!-- KPI Cards (Demo) -->
    <section class="kpi-grid" aria-label="Ringkasan Keuangan">
      <article class="kpi-card">
        <div class="kpi-head">
          <div class="kpi-icon blue"><i class="fa-solid fa-wallet" aria-hidden="true"></i></div>
          <div class="kpi-title">Total Kas</div>
        </div>
        <div class="kpi-value">Rp 73.624.062</div>
        <div class="change-badge negative" data-change="-100% dari bulan lalu">
          <i class="fa-solid fa-arrow-down" aria-hidden="true"></i>
          <span>- 100% dari bulan lalu</span>
        </div>
      </article>

      <article class="kpi-card">
        <div class="kpi-head">
          <div class="kpi-icon green"><i class="fa-solid fa-arrow-trend-up" aria-hidden="true"></i></div>
          <div class="kpi-title">Pemasukan</div>
        </div>
        <div class="kpi-value" id="dash-total-masuk">Rp 2.200.000</div>
        <div class="change-badge neutral" data-change="0% dari bulan lalu">
          <i class="fa-solid fa-minus" aria-hidden="true"></i>
          <span>0% dari bulan lalu</span>
        </div>
      </article>

      <article class="kpi-card">
        <div class="kpi-head">
          <div class="kpi-icon red"><i class="fa-solid fa-sack-xmark" aria-hidden="true"></i></div>
          <div class="kpi-title">Pengeluaran</div>
        </div>
        <div class="kpi-value" id="dash-total-keluar">Rp 1.100.000</div>
        <div class="change-badge negative" data-change="-100% dari bulan lalu">
          <i class="fa-solid fa-arrow-down" aria-hidden="true"></i>
          <span>- 100% dari bulan lalu</span>
        </div>
      </article>

      <article class="kpi-card">
        <div class="kpi-head">
          <div class="kpi-icon gold"><i class="fa-solid fa-clock" aria-hidden="true"></i></div>
          <div class="kpi-title">Tertunda</div>
        </div>
        <div class="kpi-value" id="dash-iuran-pending">Rp 800.000</div>
        <div class="change-badge neutral" data-change="Sama dengan bulan lalu">
          <i class="fa-solid fa-minus" aria-hidden="true"></i>
          <span>— Sama dengan bulan lalu</span>
        </div>
      </article>
    </section>
  </main>
</Layout>

<script>
  // Demo dashboard - no authentication required
  document.addEventListener('DOMContentLoaded', function() {
    // Demo user info
    const welcomeCard = document.querySelector('.welcome-card h2');
    if (welcomeCard) {
      welcomeCard.textContent = 'Selamat datang di Demo Dashboard!';
    }
    
    const welcomeText = document.querySelector('.welcome-card p');
    if (welcomeText) {
      welcomeText.textContent = 'Ini adalah demo dashboard KomplekKita. Anda dapat menjelajahi semua fitur tanpa perlu login.';
    }
    
    // Logout functionality (demo)
    const logoutBtn = document.getElementById('logoutBtn');
    if (logoutBtn) {
      logoutBtn.addEventListener('click', function() {
        alert('Ini adalah demo - logout tidak diperlukan');
        window.location.href = '/';
      });
    }

    // Mark 100% change badges for special animation
    try {
      document.querySelectorAll('.change-badge').forEach((el) => {
        const text = (el.textContent || el.getAttribute('data-change') || '').toLowerCase();
        if (text.includes('100%')) el.classList.add('isMax');
        if (text.includes('%')) el.classList.add('hasPercent');
      });
    } catch {}
  });
</script>

<style>
  .dashboard-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
    min-height: 100vh;
  }
  
  .dashboard-header {
    text-align: center;
    margin-bottom: 3rem;
  }
  
  .dashboard-header h1 {
    font-size: 2.5rem;
    color: #2d3748;
    margin-bottom: 0.5rem;
  }
  
  .dashboard-header p {
    font-size: 1.1rem;
    color: #4a5568;
  }
  
  .dashboard-content {
    display: flex;
    justify-content: center;
  }

  /* KPI Cards */
  .kpi-grid {
    margin-top: 2rem;
    display: grid;
    grid-template-columns: repeat(4, minmax(220px, 1fr));
    gap: 1rem;
  }

  .kpi-card {
    position: relative;
    background: #fff;
    border-radius: 16px;
    padding: 1rem 1rem 0.85rem;
    box-shadow: 0 8px 20px rgba(2,6,23,0.06);
    border: 1px solid rgba(2,6,23,0.06);
    overflow: hidden;
    isolation: isolate;
    transition: transform .2s ease, box-shadow .2s ease;
  }
  .kpi-card:hover { transform: translateY(-2px); box-shadow: 0 12px 26px rgba(2,6,23,0.10) }

  .kpi-head { display:flex; align-items:center; gap:.6rem; margin-bottom:.4rem }
  .kpi-icon { width:36px; height:36px; border-radius:10px; display:grid; place-items:center; color:#fff; }
  .kpi-icon.blue { background: linear-gradient(135deg,#60a5fa,#3b82f6) }
  .kpi-icon.green{ background: linear-gradient(135deg,#34d399,#10b981) }
  .kpi-icon.red  { background: linear-gradient(135deg,#f87171,#ef4444) }
  .kpi-icon.gold { background: linear-gradient(135deg,#fbbf24,#f59e0b) }
  .kpi-title { font-weight:600; color:#0f172a }
  .kpi-value { font-size:1.4rem; font-weight:700; color:#0b1220; letter-spacing:.2px; }

  .change-badge {
    margin-top:.6rem;
    display:inline-flex;
    align-items:center;
    gap:.5rem;
    font-size:.82rem;
    line-height:1;
    padding:.55rem .7rem;
    border-radius:10px;
    border:1px solid transparent;
    background: #f1f5f9;
    color:#0f172a;
    position:relative;
    overflow:hidden;
  }
  .change-badge i { font-size:.9rem }
  .change-badge.positive { background:#ecfdf5; color:#065f46; border-color:#a7f3d0 }
  .change-badge.negative { background:#fef2f2; color:#991b1b; border-color:#fecaca }
  .change-badge.neutral  { background:#f1f5f9; color:#334155; border-color:#e2e8f0 }

  /* Subtle slide-in on mount */
  .change-badge.hasPercent { animation: slideInUp .35s ease both }
  @keyframes slideInUp { from { opacity:0; transform: translateY(6px) } to { opacity:1; transform: translateY(0) } }

  /* Premium animation for 100% change */
  .change-badge.isMax {
    border-color: currentColor;
    box-shadow: 0 0 0 3px rgba(250, 204, 21, 0.15), 0 8px 18px rgba(2,6,23,0.08);
  }
  .change-badge.isMax::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(60% 100% at 10% 0%, rgba(255,255,255,.7), transparent 50%),
                radial-gradient(40% 80%  at 90% 20%, rgba(255,255,255,.4), transparent 60%);
    pointer-events: none;
    mix-blend-mode: overlay;
    animation: pulseGlow 2.2s ease-in-out infinite;
  }
  .change-badge.isMax span { position:relative; z-index:1 }
  .change-badge.isMax i { animation: wobble .9s ease-in-out 1 .15s }
  .change-badge.isMax { background: linear-gradient(0deg, rgba(254,226,226,.9), rgba(254,242,242,1)); }

  @keyframes pulseGlow {
    0%   { opacity:.45; }
    50%  { opacity:.9; filter: drop-shadow(0 0 10px rgba(248,113,113,.35)); }
    100% { opacity:.45; }
  }
  @keyframes wobble {
    0% { transform: translateX(0) rotate(0) }
    15%{ transform: translateX(-2px) rotate(-6deg) }
    30%{ transform: translateX(2px)  rotate(5deg) }
    45%{ transform: translateX(-1px) rotate(-4deg) }
    60%{ transform: translateX(1px)  rotate(3deg) }
    75%{ transform: translateX(-1px) rotate(-2deg) }
    100%{ transform: translateX(0)   rotate(0) }
  }

  /* Shimmer line passing through text for 100% */
  .change-badge.isMax span::after {
    content:''; position:absolute; left:-120%; top:50%; height:1.1em; width:120%; transform: translateY(-50%) skewX(-20deg);
    background: linear-gradient(90deg, transparent, rgba(255,255,255,.6), transparent);
    animation: shine 1.8s ease-in-out infinite .6s;
  }
  @keyframes shine { 0%{ left:-120% } 60%{ left:120% } 100%{ left:120% } }
  
  .welcome-card {
    background: white;
    border-radius: 12px;
    padding: 2rem;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    max-width: 600px;
    width: 100%;
    text-align: center;
  }
  
  .welcome-card h2 {
    color: #667eea;
    margin-bottom: 1rem;
    font-size: 1.8rem;
  }
  
  .welcome-card p {
    color: #4a5568;
    margin-bottom: 2rem;
    line-height: 1.6;
  }
  
  .action-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  
  .btn {
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    border: none;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  
  .btn-primary {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
  }
  
  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
  }
  
  .btn-secondary {
    background: #e2e8f0;
    color: #4a5568;
  }
  
  .btn-secondary:hover {
    background: #cbd5e0;
  }
  
  @media (max-width: 640px) {
    .dashboard-container {
      padding: 1rem;
    }
    
    .welcome-card {
      padding: 1.5rem;
    }
    
    .action-buttons {
      flex-direction: column;
    }
    .kpi-grid { grid-template-columns: 1fr 1fr }
  }
</style>
